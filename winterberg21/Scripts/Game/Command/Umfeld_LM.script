// ## 
// ## Winterberger für EM4 Scripting
// ## Version 2.0
// ## 
// ## Script Umfeld_LM
// ## 
// ## Stand : 06.8.2006
// ## 
// ## created by Chidea
// ## 
 
 // Hier muss der Name(!) des Bildes aus dem UI-Ordner stehen.
 const char IMG[] = "LightOn";
 
 const char DUMMY_HASUMFELD[] = "DUMMYHasUmfeld";
 
 int DummyGroup = 20;
 
 // 1.0
 object Umfeld_LM : CommandScript
 {
 	Umfeld_LM()
 	{
 		SetIcon(IMG);
 		SetCursor(IMG);
 		SetRestrictions(RESTRICT_SELFEXECUTE);
 	}
 
 	bool CheckPossible(GameObject *Caller)
 	{
 		if (!Caller->IsValid())
 			return false;
 
 		if (Caller->GetType() == ACTOR_VEHICLE)
 		{
 			return true;
 		}
 
 		return false;
 	}
 
 	bool CheckTarget(GameObject *Caller, Actor *Target, int childID)
 	{
 		if (!Caller->IsValid() || !Target->IsValid() || Target->GetID() != Caller->GetID())
 			return false;
 
 		if (Caller->GetType() == ACTOR_VEHICLE)
 		{
 			return true;
 		}
 
 		return true;
 	}
 
	void PushActions(GameObject *Caller, Actor *Target, int childID)
 	{
 		Vehicle v(Caller);
 
 		if (v.HasCommand(DUMMY_HASUMFELD))
 		{
 			v.EnableSpecialLights(false);
 			v.RemoveCommand(DUMMY_HASUMFELD);
			v.PlayAnimCloseDoor(DAT_SPECIAL, 3.0f);
 			return;
 		}
  
 		if (!v.HasCommand(DUMMY_HASUMFELD))
 		{
 			v.EnableSpecialLights(true);
 			v.AssignCommand(DUMMY_HASUMFELD);
			v.PlayAnimOpenDoor(DAT_SPECIAL, 3.0f);
  			return;
 		}
 	}	
 };