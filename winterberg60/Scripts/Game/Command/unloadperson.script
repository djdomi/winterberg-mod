// ## 
// ## Winterberger 4 Scripting
// ## Version 6.0
// ## 
// ## Script unloadperson
// ## 
// ## Release 07.07.2008
// ## 
// ## created by WitchDoctor




object UnloadPerson : CommandScript
{

	UnloadPerson()
	{
		SetValidTargets(ACTOR_PERSON);
		SetDoubleClickable(true);
		SetGroupID(CGROUP_CARRY_PERSON);
		SetPriority(500);
	}

	bool CheckGroupVisibility(GameObject *Caller)
	{
		if(!Caller->IsValid() || Caller->GetType() != ACTOR_PERSON)
			return false;
		Person p(Caller);
		return p.IsCarryingPerson();
	}

	bool CheckTarget(GameObject *Caller, Actor *Target, int childID)
	{
		if(!Caller->IsValid() || !Target->IsValid())
			return false;
		
		Person p(Caller);
		Person t = p.GetCarried();

		if(!p.IsValid() || !t.IsValid() || p.GetEnteredCarID() != -1)
			return false;

		// nicht im Wasser (Taucher)
		if (p.IsInsideWater())
			return false;

		if(p.GetID()== Target->GetID() || Target->GetID()== t.GetID())
			return true;
		
				
		return false;
	}

	void PushActions(GameObject *Caller, Actor *Target, int childID)
	{
		Person p(Caller);
		bool rettung=false;
		switch (p.GetPersonType())
		{
			case PT_FIREFIGHTER_NORMAL:
			case PT_FIREFIGHTER_ABC:
			case PT_FIREFIGHTER_MASK:
				VehicleList vl(Caller->GetName());
				Vehicle v=vl.GetVehicle(0);
				rettung=true;
				break;
		}
		if (p.IsCarryingPerson())
		{
			Caller->PushActionUnloadPerson(ACTION_APPEND);
			if (rettung)
			{
				if (childID==1)
				{
					Vector pos=Caller->GetPosition();
					pos.x+=75;
					pos.y+=75;
					Game::FindFreePosition(Caller,pos,150.0);
					Caller->PushActionMove(ACTION_APPEND,pos,true);
				}
				Caller->PushActionExecuteCommand(ACTION_APPEND,"UnloadPerson",Target,1,false);
			}
		} else 
			if (rettung)
				Caller->PushActionMove(ACTION_APPEND,&v,TARGET_REARDOOR);
	}
};
